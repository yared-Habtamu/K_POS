import 'package:flutter/material.dart';
import 'package:pos_app/features/owners_page/presentation/pages/owner_inventory_page.dart';

/// Manager inventory wraps owner inventory UI for now.
class ManagerInventoryPage extends StatelessWidget {
  const ManagerInventoryPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const OwnerInventoryPage();
  }
}
