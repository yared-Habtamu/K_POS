This folder contains the role-based dashboard shell for the mobile app.

- `RoleDashboardPage` shows a drawer (sidebar) and placeholder pages per role.
- Roles supported: `system_admin`, `owner`, `manager`, `cashier`, `store_keeper`.
