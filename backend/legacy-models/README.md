Archived legacy Mongoose model files.

These files were archived during the MongoDB→Postgres code migration. They are preserved for reference only.

To restore an original model from Git history, run:
git checkout HEAD@{1} -- backend/src/models/<model-file>

Files archived below are placeholders containing the original filename and a short note.
