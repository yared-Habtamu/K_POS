version: "3.8"
services:
  db:
    image: postgres:15
    restart: always
    environment:
      POSTGRES_USER: kiya
      POSTGRES_PASSWORD: kiya_pass
      POSTGRES_DB: kiya_pos
    volumes:
      - db-data:/var/lib/postgresql/data
    ports:
      - "5434:5432"

volumes:
  db-data:
